# Task Management Application
Full-stack task manager with authentication, authorization and CRUD operations.
Stack: HTML, CSS, JavaScript, Node.js, Express, SQLite, JWT and bcrypt.
Run `npm install`, then `npm start`, then open http://localhost:3001.
Each account manages its own tasks. Features include create, update, delete, status, priority, due date and responsive design.
